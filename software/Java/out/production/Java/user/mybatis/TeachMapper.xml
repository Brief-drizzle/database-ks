<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="user.mapper.TeachMapper">
	<resultMap id="BaseResultMap" type="user.entity.Teach">
            <result column="instructor_id" property="instructorId" />
	        <result column="course_id" property="courseId" />
		</resultMap>  
    
    <!-- 表字段 -->
    <sql id="baseColumns">
         t.instructor_id
        , t.course_id
        </sql> 
    
    <!-- 查询全部 -->
    <select id="listAll" resultMap="BaseResultMap">
		SELECT         
        	<include refid="baseColumns" />
		FROM dbo.teach t
	</select>
 
 	<!-- 根据主键获取单条记录 -->
    <select id="getById" resultMap="BaseResultMap" parameterType="String">
		SELECT         
        	<include refid="baseColumns" />
		FROM dbo.teach t
		WHERE instructor_id = #{instructorId}
	</select>

	<!-- 插入全部字段 -->
    <insert id="insert" parameterType="user.entity.Teach"
		keyProperty="instructorId" keyColumn="instructor_id" useGeneratedKeys="true"
    >
	INSERT INTO dbo.teach
    <trim prefix="(" suffix=")" suffixOverrides=",">	 
        	    	instructor_id,
    	        	    	course_id,
    	        </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides=",">            
        	        #{instructorId},                   
    	        	        #{courseId},                   
    	        </trim>
	</insert>
    
    <!-- 插入不为NULL的字段 -->
    <insert id="insertIgnoreNull" parameterType="user.entity.Teach"
        keyProperty="instructorId" keyColumn="instructor_id" useGeneratedKeys="true"
        >
        INSERT INTO dbo.teach    
        <trim prefix="(" suffix=")" suffixOverrides=",">	 

                                                <if test="instructorId != null">
                   instructor_id,
                    </if>
                                                                <if test="courseId != null">
                   course_id,
                    </if>
                                    </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">            
                                                <if test="instructorId != null" >
                    #{instructorId},                    
                    </if>
                                                                <if test="courseId != null" >
                    #{courseId},                    
                    </if>
                                    </trim>
    </insert>

	<!-- 更新,更新全部字段 -->
    <update id="update" parameterType="user.entity.Teach">
    UPDATE dbo.teach
     <set>		
                                      </set>	
    WHERE instructor_id = #{instructorId}
    </update>  
    
    
    <!-- 更新不为NULL的字段 -->
    <update id="updateIgnoreNull" parameterType="user.entity.Teach">
    UPDATE dbo.teach
    <set>
    	                                            </set>
    WHERE instructor_id = #{instructorId}
    </update>

		
	<!-- 根据主键删除记录 -->
	<delete id="delete" parameterType="user.entity.Teach">
		UPDATE dbo.teach
		SET is_deleted=1
		WHERE instructor_id = #{instructorId}
	</delete>


</mapper>