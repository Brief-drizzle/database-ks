<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="user.mapper.CategoryMapper">
	<resultMap id="BaseResultMap" type="user.entity.Category">
            <result column="category_id" property="categoryId" />
	        <result column="category_name" property="categoryName" />
		</resultMap>  
    
    <!-- 表字段 -->
    <sql id="baseColumns">
         t.category_id
        , t.category_name
        </sql> 
    
    <!-- 查询全部 -->
    <select id="listAll" resultMap="BaseResultMap">
		SELECT         
        	<include refid="baseColumns" />
		FROM dbo.category t
	</select>
 
 	<!-- 根据主键获取单条记录 -->
    <select id="getById" resultMap="BaseResultMap" parameterType="String">
		SELECT         
        	<include refid="baseColumns" />
		FROM dbo.category t
		WHERE category_id = #{categoryId}
	</select>

	<!-- 插入全部字段 -->
    <insert id="insert" parameterType="user.entity.Category"
		keyProperty="categoryId" keyColumn="category_id" useGeneratedKeys="true"
    >
	INSERT INTO dbo.category
    <trim prefix="(" suffix=")" suffixOverrides=",">	 
        	    	category_id,
    	        	    	category_name,
    	        </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides=",">            
        	        #{categoryId},                   
    	        	        #{categoryName},                   
    	        </trim>
	</insert>
    
    <!-- 插入不为NULL的字段 -->
    <insert id="insertIgnoreNull" parameterType="user.entity.Category"
        keyProperty="categoryId" keyColumn="category_id" useGeneratedKeys="true"
        >
        INSERT INTO dbo.category    
        <trim prefix="(" suffix=")" suffixOverrides=",">	 

                                                <if test="categoryId != null">
                   category_id,
                    </if>
                                                                <if test="categoryName != null">
                   category_name,
                    </if>
                                    </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">            
                                                <if test="categoryId != null" >
                    #{categoryId},                    
                    </if>
                                                                <if test="categoryName != null" >
                    #{categoryName},                    
                    </if>
                                    </trim>
    </insert>

	<!-- 更新,更新全部字段 -->
    <update id="update" parameterType="user.entity.Category">
    UPDATE dbo.category
     <set>		
                                     category_name=#{categoryName},        
                   </set>	
    WHERE category_id = #{categoryId}
    </update>  
    
    
    <!-- 更新不为NULL的字段 -->
    <update id="updateIgnoreNull" parameterType="user.entity.Category">
    UPDATE dbo.category
    <set>
    	                                                <if test="categoryName != null" >
                category_name=#{categoryName},                 
                </if>
                        </set>
    WHERE category_id = #{categoryId}
    </update>

		
	<!-- 根据主键删除记录 -->
	<delete id="delete" parameterType="user.entity.Category">
		UPDATE dbo.category
		SET is_deleted=1
		WHERE category_id = #{categoryId}
	</delete>


</mapper>