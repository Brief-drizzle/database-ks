<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="user.dao.InstructorMapper">
	<resultMap id="BaseResultMap" type="user.entity.Instructor">
            <result column="instructor_id" property="instructorId" />
	        <result column="teacher_id" property="teacherId" />
	        <result column="administrator_id" property="administratorId" />
	        <result column="isinstructor" property="isinstructor" />
		</resultMap>  
    
    <!-- 表字段 -->
    <sql id="baseColumns">
         t.instructor_id
        , t.teacher_id
        , t.administrator_id
        , t.isinstructor
        </sql> 
    
    <!-- 查询全部 -->
    <select id="listAll" resultMap="BaseResultMap">
		SELECT         
        	<include refid="baseColumns" />
		FROM dbo.instructor t
	</select>
 
 	<!-- 根据主键获取单条记录 -->
    <select id="getById" resultMap="BaseResultMap" parameterType="String">
		SELECT         
        	<include refid="baseColumns" />
		FROM dbo.instructor t
		WHERE instructor_id = #{instructorId}
	</select>

	<!-- 插入全部字段 -->
    <insert id="insert" parameterType="user.entity.Instructor"
		keyProperty="instructorId" keyColumn="instructor_id" useGeneratedKeys="true"
    >
	INSERT INTO dbo.instructor
    <trim prefix="(" suffix=")" suffixOverrides=",">	 
        	    	instructor_id,
    	        	    	teacher_id,
    	        	    	administrator_id,
    	        	    	isinstructor,
    	        </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides=",">            
        	        #{instructorId},                   
    	        	        #{teacherId},                   
    	        	        #{administratorId},                   
    	        	        #{isinstructor},                   
    	        </trim>
	</insert>
    
    <!-- 插入不为NULL的字段 -->
    <insert id="insertIgnoreNull" parameterType="user.entity.Instructor"
        keyProperty="instructorId" keyColumn="instructor_id" useGeneratedKeys="true"
        >
        INSERT INTO dbo.instructor    
        <trim prefix="(" suffix=")" suffixOverrides=",">	 

                                                <if test="instructorId != null">
                   instructor_id,
                    </if>
                                                                <if test="teacherId != null">
                   teacher_id,
                    </if>
                                                                <if test="administratorId != null">
                   administrator_id,
                    </if>
                                                                <if test="isinstructor != null">
                   isinstructor,
                    </if>
                                    </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">            
                                                <if test="instructorId != null" >
                    #{instructorId},                    
                    </if>
                                                                <if test="teacherId != null" >
                    #{teacherId},                    
                    </if>
                                                                <if test="administratorId != null" >
                    #{administratorId},                    
                    </if>
                                                                <if test="isinstructor != null" >
                    #{isinstructor},                    
                    </if>
                                    </trim>
    </insert>

	<!-- 更新,更新全部字段 -->
    <update id="update" parameterType="user.entity.Instructor">
    UPDATE dbo.instructor
     <set>		
                                     teacher_id=#{teacherId},        
                                administrator_id=#{administratorId},        
                                isinstructor=#{isinstructor},        
                   </set>	
    WHERE instructor_id = #{instructorId}
    </update>  
    
    
    <!-- 更新不为NULL的字段 -->
    <update id="updateIgnoreNull" parameterType="user.entity.Instructor">
    UPDATE dbo.instructor
    <set>
    	                                                <if test="teacherId != null" >
                teacher_id=#{teacherId},                 
                </if>
                                                <if test="administratorId != null" >
                administrator_id=#{administratorId},                 
                </if>
                                                <if test="isinstructor != null" >
                isinstructor=#{isinstructor},                 
                </if>
                        </set>
    WHERE instructor_id = #{instructorId}
    </update>

		
	<!-- 根据主键删除记录 -->
	<delete id="delete" parameterType="user.entity.Instructor">
		UPDATE dbo.instructor
		SET is_deleted=1
		WHERE instructor_id = #{instructorId}
	</delete>


</mapper>