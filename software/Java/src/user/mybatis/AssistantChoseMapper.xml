<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="user.dao.AssistantChoseMapper">
	<resultMap id="BaseResultMap" type="user.entity.AssistantChose">
            <result column="course_id" property="courseId" />
	        <result column="assessment_id" property="assessmentId" />
		</resultMap>  
    
    <!-- 表字段 -->
    <sql id="baseColumns">
         t.course_id
        , t.assessment_id
        </sql> 
    
    <!-- 查询全部 -->
    <select id="listAll" resultMap="BaseResultMap">
		SELECT         
        	<include refid="baseColumns" />
		FROM dbo.assistant_chose t
	</select>
 
 	<!-- 根据主键获取单条记录 -->
    <select id="getById" resultMap="BaseResultMap" parameterType="String">
		SELECT         
        	<include refid="baseColumns" />
		FROM dbo.assistant_chose t
		WHERE course_id = #{courseId}
	</select>

	<!-- 插入全部字段 -->
    <insert id="insert" parameterType="user.entity.AssistantChose"
		keyProperty="courseId" keyColumn="course_id" useGeneratedKeys="true"
    >
	INSERT INTO dbo.assistant_chose
    <trim prefix="(" suffix=")" suffixOverrides=",">	 
        	    	course_id,
    	        	    	assessment_id,
    	        </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides=",">            
        	        #{courseId},                   
    	        	        #{assessmentId},                   
    	        </trim>
	</insert>
    
    <!-- 插入不为NULL的字段 -->
    <insert id="insertIgnoreNull" parameterType="user.entity.AssistantChose"
        keyProperty="courseId" keyColumn="course_id" useGeneratedKeys="true"
        >
        INSERT INTO dbo.assistant_chose    
        <trim prefix="(" suffix=")" suffixOverrides=",">	 

                                                <if test="courseId != null">
                   course_id,
                    </if>
                                                                <if test="assessmentId != null">
                   assessment_id,
                    </if>
                                    </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">            
                                                <if test="courseId != null" >
                    #{courseId},                    
                    </if>
                                                                <if test="assessmentId != null" >
                    #{assessmentId},                    
                    </if>
                                    </trim>
    </insert>

	<!-- 更新,更新全部字段 -->
    <update id="update" parameterType="user.entity.AssistantChose">
    UPDATE dbo.assistant_chose
     <set>		
                                      </set>	
    WHERE course_id = #{courseId}
    </update>  
    
    
    <!-- 更新不为NULL的字段 -->
    <update id="updateIgnoreNull" parameterType="user.entity.AssistantChose">
    UPDATE dbo.assistant_chose
    <set>
    	                                            </set>
    WHERE course_id = #{courseId}
    </update>

		
	<!-- 根据主键删除记录 -->
	<delete id="delete" parameterType="user.entity.AssistantChose">
		UPDATE dbo.assistant_chose
		SET is_deleted=1
		WHERE course_id = #{courseId}
	</delete>


</mapper>